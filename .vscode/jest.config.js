export default {
  // Configure how files are transformed before testing; currently empty (no transformations applied)
  transform: {},

  // Specify the testing environment; "node" means tests will run in a Node.js-like environment
  testEnvironment: "node",

  // Treat specified file extensions as ECMAScript modules (ESM)
  extensionsToTreatAsEsm: [".js"],
};
